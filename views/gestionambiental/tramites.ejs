<% 
extraStyles=`
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<link rel="stylesheet" href="css/sidebar.css" />
<style>
  .app-container {
    display: flex;
    min-height: 100vh;
    flex-direction: row;
  }
  
  .main-content {
    flex: 1;
    padding: 20px;
    background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
    min-height: 100vh;
    overflow-x: hidden;
  }
  
  .content-header {
    background: white;
    border-radius: 15px;
    padding: 25px;
    margin-bottom: 25px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    border-left: 5px solid #7c1241;
  }
  
  .content-header h3 {
    color: #2c3e50;
    font-weight: 600;
    margin: 0;
    display: flex;
    align-items: center;
    gap: 12px;
  }
  
  .content-header .icon {
    background: linear-gradient(135deg, #7c1241, #9c1a5a);
    color: white;
    padding: 12px;
    border-radius: 12px;
    font-size: 1.2em;
  }
  
  .action-buttons {
    margin-top: 20px;
  }
  
  .btn-primary-custom {
    background: linear-gradient(135deg, #7c1241, #9c1a5a);
    border: none;
    color: white;
    padding: 12px 24px;
    border-radius: 25px;
    font-weight: 500;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(124, 18, 65, 0.3);
  }
  
  .btn-primary-custom:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(124, 18, 65, 0.4);
    color: white;
  }
  
  .filters-section {
    background: white;
    border-radius: 15px;
    padding: 20px;
    margin-bottom: 25px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  /* Permitir que el dropdown de sugerencias del filtro salga del contenedor */
  overflow: visible;
  }
  
  .filters-section h6 {
    color: #2c3e50;
    font-weight: 600;
    margin-bottom: 15px;
    display: flex;
    align-items: center;
    gap: 10px;
  }
  
  .filters-section .icon {
    background: linear-gradient(135deg, #3498db, #2980b9);
    color: white;
    padding: 6px;
    border-radius: 6px;
    font-size: 0.8em;
  }
  
  .form-check {
    margin-bottom: 8px;
  }
  
  .form-check-input:checked {
    background-color: #7c1241;
    border-color: #7c1241;
  }
  
  .form-check-label {
    color: #34495e;
    font-weight: 500;
    cursor: pointer;
  }
  
  .table-container {
    background: white;
    border-radius: 15px;
    padding: 25px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    overflow: hidden;
  }
  
  .table {
    margin: 0;
  }
  
  .table thead th {
    background: white;
    border: none;
    color: #000000;
    font-weight: 600;
    padding: 15px 12px;
    text-align: center;
    font-size: 0.9em;
  }
  
  .table tbody td {
    padding: 15px 12px;
    vertical-align: middle;
    border-bottom: 1px solid #ecf0f1;
  }
  
  .table tbody tr:hover {
    background-color: #f8f9fa;
    transition: background-color 0.3s ease;
  }
  
  .modal-content {
    border-radius: 15px;
    border: none;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
  }
  
  .modal-header {
    background: linear-gradient(135deg, #7c1241, #9c1a5a);
    border-radius: 15px 15px 0 0;
    padding: 20px 25px;
  }
  
  .modal-title {
    color: white !important;
    font-weight: 600;
  }
  
  .modal-body {
    padding: 25px;
  }
  
  .form-label {
    font-weight: 600;
    color: #2c3e50;
    margin-bottom: 8px;
  }
  
  .form-control {
    border-radius: 8px;
    border: 2px solid #ecf0f1;
    padding: 12px 15px;
    transition: all 0.3s ease;
  }
  
  .form-control:focus {
    border-color: #7c1241;
    box-shadow: 0 0 0 0.2rem rgba(124, 18, 65, 0.25);
  }
  
  .form-check-input {
    width: 18px;
    height: 18px;
    margin-top: 2px;
  }
  
  .btn-close {
    filter: invert(1);
  }
  
  /* Stats Cards */
  .stats-card {
    background: white;
    border-radius: 12px;
    padding: 15px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    display: flex;
    align-items: center;
    gap: 12px;
    transition: transform 0.2s ease;
  }
  
  .stats-card:hover {
    transform: translateY(-2px);
  }
  
  .stats-icon {
    width: 40px;
    height: 40px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.2em;
  }
  
  .stats-info {
    display: flex;
    flex-direction: column;
  }
  
  .stats-number {
    font-size: 1.5em;
    font-weight: 700;
    color: #2c3e50;
    line-height: 1;
  }
  
  .stats-label {
    font-size: 0.8em;
    color: #7f8c8d;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }
  
  /* Pagination */
  .pagination .page-link {
    border-radius: 6px;
    margin: 0 2px;
    border: 1px solid #dee2e6;
    color: #7c1241;
  }
  
  .pagination .page-link:hover {
    background-color: #7c1241;
    border-color: #7c1241;
    color: white;
  }
  
  .pagination .page-item.active .page-link {
    background-color: #7c1241;
    border-color: #7c1241;
  }
  
  /* Loading states */
  .btn:disabled {
    opacity: 0.6;
    cursor: not-allowed;
  }
  
  /* Filter buttons */
  .btn-outline-primary {
    border-color: #7c1241;
    color: #7c1241;
  }
  
  .btn-outline-primary:hover,
  .btn-outline-primary:focus,
  .btn-outline-primary:active {
    background-color: #7c1241;
    border-color: #7c1241;
    color: white;
  }
  
  .btn-check:checked + .btn-outline-primary {
    background-color: #7c1241;
    border-color: #7c1241;
    color: white;
  }
  
  /* Status badges */
  .status-badge {
    padding: 6px 12px;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 500;
    text-align: center;
    min-width: 120px;
    display: inline-block;
  }
  
  .status-ingresado { background: #dbeafe; color: #1e40af; }
  .status-turnado { background: #fef3c7; color: #d97706; }
  .status-firma { background: #fce7f3; color: #be185d; }
  .status-direccion { background: #e0e7ff; color: #7c3aed; }
  .status-resguardo { background: #f3e8ff; color: #a855f7; }
  .status-notificado { background: #dcfce7; color: #15803d; }
  
  /* Action buttons */
  .btn-accion {
    padding: 6px 10px;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-size: 0.8rem;
    transition: all 0.3s ease;
    margin: 0 2px;
  }
  
  .btn-ver { background: #3b82f6; color: white; }
  .btn-ver:hover { background: #2563eb; }
  .btn-editar { background: #f59e0b; color: white; }
  .btn-editar:hover { background: #d97706; }
  .btn-eliminar { background: #ef4444; color: white; }
  .btn-eliminar:hover { background: #dc2626; }
  
  @media (max-width: 1200px) {
    .stats-card {
      margin-bottom: 15px;
    }
  }
  
  @media (max-width: 768px) {
    .main-content {
      padding: 10px;
    }
    
    .content-header, .filters-section, .table-container {
      padding: 15px;
    }
    
    .content-header h3 {
      font-size: 1.5em;
      flex-direction: column;
      gap: 8px;
      text-align: center;
    }
    
    .action-buttons {
      margin-top: 15px;
      text-align: center;
    }
    
    .btn-primary-custom {
      width: 100%;
      max-width: 300px;
    }
    
    .filters-section .row {
      margin: 0;
    }
    
    .filters-section .col-md-4,
    .filters-section .col-md-2 {
      margin-bottom: 15px;
    }
    
    .filters-section .d-flex.justify-content-between {
      flex-direction: column;
      gap: 15px;
    }
    
    .filters-section .d-flex.justify-content-between > div {
      width: 100%;
      text-align: center;
    }
    
    .btn-group {
      flex-wrap: wrap;
      justify-content: center;
    }
    
    .btn-group .btn {
      margin: 2px;
    }
    
    .stats-card {
      padding: 10px;
      margin-bottom: 10px;
    }
    
    .stats-icon {
      width: 30px;
      height: 30px;
      font-size: 0.9em;
    }
    
    .stats-number {
      font-size: 1.1em;
    }
    
    .stats-label {
      font-size: 0.7em;
    }
    
    .table-responsive {
      font-size: 0.8em;
    }
    
    .table thead th,
    .table tbody td {
      padding: 8px 6px;
    }
    
    .pagination {
      justify-content: center;
      flex-wrap: wrap;
    }
    
    .pagination .page-link {
      padding: 6px 10px;
      font-size: 0.9em;
    }
    
    .d-flex.justify-content-between.align-items-center {
      flex-direction: column;
      gap: 15px;
      text-align: center;
    }
    
    .d-flex.justify-content-between.align-items-center > div {
      width: 100%;
    }
    
    #limitSelect {
      width: 100%;
      max-width: 200px;
      margin: 0 auto;
    }
  }
  
  @media (max-width: 576px) {
    .main-content {
      padding: 8px;
    }
    
    .content-header, .filters-section, .table-container {
      padding: 12px;
    }
    
    .content-header h3 {
      font-size: 1.3em;
    }
    
    .stats-card {
      padding: 8px;
    }
    
    .stats-icon {
      width: 25px;
      height: 25px;
      font-size: 0.8em;
    }
    
    .stats-number {
      font-size: 1em;
    }
    
    .stats-label {
      font-size: 0.65em;
    }
    
    .btn-primary-custom {
      padding: 10px 16px;
      font-size: 0.9em;
    }
    
    .table-responsive {
      font-size: 0.75em;
    }
    
    .table thead th,
    .table tbody td {
      padding: 6px 4px;
    }
    
    .btn-group .btn {
      padding: 4px 6px;
      font-size: 0.8em;
    }
    
    .content-header h3 {
      font-size: 1.2em;
    }
    
    .content-header p {
      font-size: 0.9em;
    }
  }
  
  /* Asegurar que no haya desbordamiento horizontal */
  .filters-section,
  .table-container,
  .content-header {
    max-width: 100%;
    overflow-x: hidden;
  }
  
  /* Prevenir padding residual del modal */
  body {
    overflow-x: hidden;
  }
  
  /* Asegurar que el main content mantenga su padding */
  .main-content {
    padding-right: 20px;
  }
  
  /* Prevenir scroll horizontal */
  .app-container {
    overflow-x: hidden;
    max-width: 100vw;
  }
  
  /* Prevenir que Bootstrap modifique el body cuando el modal esté abierto */
  body.modal-open {
    padding-right: 0 !important;
  }
  
  /* Asegurar que el modal funcione correctamente */
  .modal {
    z-index: 1050;
  }
  
  /* Modal cuando está abierto */
  .modal.show {
    display: block !important;
    pointer-events: auto !important;
    z-index: 1055 !important;
  }
  
  /* Backdrop del modal */
  .modal-backdrop.show {
    opacity: 0.5 !important;
    pointer-events: auto !important;
    z-index: 1050 !important;
  }
  
  /* Asegurar que el contenido del modal sea interactivo */
  .modal-content {
    pointer-events: auto !important;
    position: relative !important;
    z-index: 1056 !important;
  }
  
  /* Asegurar que los botones del modal sean clickeables */
  .modal .btn {
    pointer-events: auto !important;
    position: relative !important;
    z-index: 1057 !important;
  }
  
  /* Asegurar que los inputs del modal sean interactivos */
  .modal input,
  .modal select,
  .modal textarea {
    pointer-events: auto !important;
    position: relative !important;
    z-index: 1057 !important;
  }
  
  /* Prevenir scroll en el body cuando el modal esté abierto */
  body:has(.modal.show) {
    overflow: hidden;
  }
  
  /* Tabla estática sin scroll horizontal */
  .table-responsive {
    border: 0;
    overflow-x: visible;
    width: 100%;
  }
  
  .table {
    width: 100%;
    table-layout: fixed;
    margin: 0;
  }
  
  .table thead th,
  .table tbody td {
    word-wrap: break-word;
    overflow-wrap: break-word;
    white-space: normal;
    vertical-align: middle;
    border-bottom: 1px solid #dee2e6;
  }
  
  .table thead th {
    background: #f8f9fa;
    font-weight: 600;
    color: #495057;
    border-top: none;
    border-bottom: 2px solid #dee2e6;
  }
  
  /* Definir anchos específicos para cada columna */
  .table th:nth-child(1), .table td:nth-child(1) { width: 15%; } /* Folio */
  .table th:nth-child(2), .table td:nth-child(2) { width: 25%; } /* Empresa */
  .table th:nth-child(3), .table td:nth-child(3) { width: 15%; } /* Fecha */
  .table th:nth-child(4), .table td:nth-child(4) { width: 15%; } /* Tipo */
  .table th:nth-child(5), .table td:nth-child(5) { width: 15%; } /* Asunto */
  .table th:nth-child(6), .table td:nth-child(6) { width: 10%; } /* Status */
  .table th:nth-child(7), .table td:nth-child(7) { width: 15%; } /* Técnicos */
  .table th:nth-child(8), .table td:nth-child(8) { width: 15%; } /* Acciones */
  
  /* Mejorar la apariencia de las celdas */
  .table tbody tr:hover {
    background-color: #f8f9fa;
  }
  
  .table tbody tr:nth-child(even) {
    background-color: #fafbfc;
  }
  
  .table tbody tr:nth-child(even):hover {
    background-color: #e9ecef;
  }
  
  /* Ajustar botones de acción en móviles */
  .btn-group-sm .btn {
    padding: 0.25rem 0.5rem;
    font-size: 0.75rem;
  }
  
  /* Asegurar que el texto largo se maneje correctamente */
  .text-truncate {
    max-width: 100%;
    display: block;
  }
  
  /* Estilos para las secciones del modal */
  .modal-body h6 {
    color: #7c1241 !important;
    font-weight: 600;
    border-bottom: 2px solid #e9ecef;
    padding-bottom: 8px;
  }
  
  .modal-body hr {
    border-color: #7c1241;
    opacity: 0.3;
    margin: 2rem 0;
  }
  
  .modal-body .form-label {
    font-weight: 500;
    color: #495057;
  }
  
  /* Estilos para el modal de visualización */
  .form-control-plaintext {
    background-color: #f8f9fa;
    border: 1px solid #e9ecef;
    border-radius: 6px;
    padding: 8px 12px;
    margin: 0;
    color: #495057;
    font-size: 0.9em;
  }
  
  .form-control-plaintext:empty::before {
    content: '-';
    color: #6c757d;
    font-style: italic;
  }
  
  .btn-outline-info {
    border-color: #17a2b8;
    color: #17a2b8;
  }
  
     .btn-outline-info:hover {
     background-color: #17a2b8;
     border-color: #17a2b8;
     color: white;
   }
   
  /* ── Tracking Cards ── */
  .tracking-card {
    background: white;
    border-radius: 10px;
    padding: 16px 20px;
    margin-bottom: 10px;
    border-left: 4px solid #dee2e6;
    transition: all 0.2s ease;
  }
  .tracking-card:hover { box-shadow: 0 2px 12px rgba(0,0,0,0.06); }

  .tracking-card.st-ingresado  { border-left-color: #3b82f6; }
  .tracking-card.st-turnado    { border-left-color: #f59e0b; }
  .tracking-card.st-firma      { border-left-color: #ec4899; }
  .tracking-card.st-direccion  { border-left-color: #0ea5e9; }
  .tracking-card.st-resguardo  { border-left-color: #f97316; }
  .tracking-card.st-notificado { border-left-color: #22c55e; }

  /* Fila 1 */
  .tc-row-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 12px;
    margin-bottom: 8px;
  }
  .tc-id {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
    min-width: 0;
  }
  .tc-folio {
    font-size: 0.88rem;
    font-weight: 700;
    color: #1e293b;
    white-space: nowrap;
  }
  .tc-meta {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-shrink: 0;
  }
  .tc-date {
    font-size: 0.75rem;
    color: #94a3b8;
    white-space: nowrap;
  }
  .tc-actions { display: flex; gap: 3px; }
  .tc-actions .btn {
    width: 28px; height: 28px; padding: 0;
    display: flex; align-items: center; justify-content: center;
    border-radius: 6px; font-size: 0.75rem;
  }

  /* Fila 2: Empresa */
  .tc-empresa {
    font-size: 0.8rem;
    color: #64748b;
    margin-bottom: 10px;
    display: flex;
    align-items: center;
    gap: 6px;
  }
  .tc-empresa i { font-size: 0.7rem; color: #94a3b8; }
  .tc-empresa strong { color: #334155; }
  .tc-empresa span { color: #64748b; }

  /* Fila 3: Progreso + % */
  .tc-progress-section {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 8px;
  }
  .tc-progress-bar {
    flex: 1; height: 5px;
    background: #f1f5f9; border-radius: 5px; overflow: hidden;
  }
  .tc-progress-fill {
    height: 100%; border-radius: 5px;
    transition: width 0.5s ease;
  }
  .tc-pct {
    font-size: 0.7rem; font-weight: 700;
    color: #475569; min-width: 32px; text-align: right;
  }
  .tc-fill-ingresado  { background: #3b82f6; }
  .tc-fill-turnado    { background: #f59e0b; }
  .tc-fill-firma      { background: #ec4899; }
  .tc-fill-direccion  { background: #0ea5e9; }
  .tc-fill-resguardo  { background: #f97316; }
  .tc-fill-notificado { background: #22c55e; }

  /* Fila 4: Stepper */
  .tc-stepper {
    display: flex; align-items: flex-start;
    margin-bottom: 6px;
  }
  .tc-step {
    display: flex; flex-direction: column;
    align-items: center; flex: 1; min-width: 0;
  }
  .tc-step-dot {
    width: 10px; height: 10px; border-radius: 50%;
    background: #e2e8f0; border: 2px solid #e2e8f0;
    z-index: 1; transition: all 0.3s;
  }
  .tc-step.done .tc-step-dot { background: #22c55e; border-color: #22c55e; }
  .tc-step.active .tc-step-dot {
    background: white; border: 2.5px solid #7c1241;
    box-shadow: 0 0 0 2px rgba(124,18,65,0.15);
    width: 12px; height: 12px;
  }
  .tc-step-label {
    font-size: 0.58rem; color: #cbd5e1;
    text-align: center; margin-top: 3px;
    line-height: 1.1; max-width: 68px; word-wrap: break-word;
  }
  .tc-step.done .tc-step-label  { color: #64748b; font-weight: 500; }
  .tc-step.active .tc-step-label { color: #1e293b; font-weight: 700; }
  .tc-step-line {
    flex: 1; height: 2px; background: #e2e8f0;
    min-width: 8px; align-self: flex-start; margin-top: 5px;
  }
  .tc-step-line.done { background: #22c55e; }

  /* Fila 5: Tecnicos */
  .tc-tecnicos {
    display: flex; gap: 6px; flex-wrap: wrap;
    padding-top: 6px; border-top: 1px solid #f1f5f9;
  }

  /* Tags */
  .tc-tag {
    font-size: 0.68rem; padding: 2px 8px;
    border-radius: 4px; font-weight: 500; white-space: nowrap;
  }
  .tc-tag.tipo    { background: #f0fdfa; color: #0f766e; }
  .tc-tag.asunto  { background: #eff6ff; color: #2563eb; }
  .tc-tag.tecnico { background: #fffbeb; color: #b45309; }
  .tc-tag.tecnico i { font-size: 0.6rem; margin-right: 3px; }

  .tc-locked { color: #ef4444; font-size: 0.72rem; }

  .tc-empty {
    text-align: center; padding: 48px 20px; color: #94a3b8;
  }
  .tc-empty i { font-size: 2.5rem; margin-bottom: 12px; display: block; }

  @media (max-width: 768px) {
    .tracking-card { padding: 12px 14px; }
    .tc-row-top { flex-direction: column; align-items: flex-start; gap: 6px; }
    .tc-meta { width: 100%; justify-content: space-between; }
    .tc-folio { font-size: 0.82rem; }
    .tc-step-label { font-size: 0.5rem; max-width: 48px; }
    .tc-actions .btn { width: 26px; height: 26px; font-size: 0.7rem; }
  }

   /* Estilos para el dropdown de sugerencias */
   .suggestions-dropdown {
     position: absolute;
     top: 100%;
     left: 0;
     right: 0;
     background: white;
     border: 1px solid #dee2e6;
     border-top: none;
     border-radius: 0 0 8px 8px;
     box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  z-index: 1200; /* por encima de tarjetas y secciones; debajo de modales (1050+) y SweetAlert */
     max-height: 200px;
     overflow-y: auto;
   }

   /* Dentro del modal, las sugerencias deben ir por encima de los campos */
   .modal .suggestions-dropdown {
     z-index: 20000 !important;
   }
   
   .suggestion-item {
     padding: 10px 15px;
     cursor: pointer;
     border-bottom: 1px solid #f8f9fa;
     transition: background-color 0.2s ease;
   }
   
   .suggestion-item:hover {
     background-color: #f8f9fa;
   }
   
   .suggestion-item:last-child {
     border-bottom: none;
   }
   
   .suggestion-item .empresa-codigo {
     font-weight: 600;
     color: #7c1241;
   }
   
   .suggestion-item .empresa-nombre {
     color: #6c757d;
     font-size: 0.9em;
   }
   
   /* Posicionamiento relativo para el contenedor de empresa */
   .empresa-container {
     position: relative;
     /* sin z-index para no sobreponerse a modales */
   }
   .empresa-filter-container {
     position: relative;
     /* sin z-index para no sobreponerse a modales */
   }

   /* Asegurar que SweetAlert2 quede por encima de todo */
   .swal2-container {
     z-index: 20000 !important;
   }
 </style>
`; %>

<div class="app-container">
  <!-- Sidebar Component -->
  <%- include('../components/sidebar', { 
    currentPage: '/gestionambiental/tramites', 
    pageTitle: 'Gestión de Trámites - Gestión Ambiental', 
    isAdmin: true
  }) %>

  <!-- Main Content -->
  <main class="main-content">
    <!-- Header Section -->
    <div class="content-header">
      <div class="d-flex justify-content-between align-items-start">
        <div>
          <h3>
            <i class="fas fa-file-alt icon"></i>
            Gestión de Trámites
          </h3>
          <p class="text-muted mb-0 mt-2">Sistema de administración de trámites del área de gestión ambiental</p>
        </div>
        
         <div class="action-buttons">
       <button id="btnNuevoTramite" class="btn btn-primary-custom">
             <i class="fas fa-plus me-2"></i>
             Nuevo Trámite
           </button>
         </div>
      </div>
    </div>

    <!-- Search and Filters Section -->
    <div class="filters-section">
      <div class="row g-3">
        <!-- Búsqueda -->
        <div class="col-lg-4 col-md-6 col-sm-12">
          <label class="form-label">
            <i class="fas fa-search me-2"></i>Búsqueda
          </label>
          <input type="text" id="searchInput" class="form-control" placeholder="Buscar por folio, empresa o observaciones...">
        </div>
        
        <!-- Empresa (buscador) -->
        <div class="col-lg-3 col-md-6 col-sm-12">
          <label class="form-label">
            <i class="fas fa-building me-2"></i>Empresa
          </label>
          <div class="empresa-filter-container">
            <input type="hidden" id="filterEmpresaId">
            <input type="text" id="filterEmpresaInput" class="form-control" placeholder="Escribe código o nombre..." autocomplete="off">
            <div id="filterEmpresaSuggestions" class="suggestions-dropdown" style="display: none;"></div>
          </div>
        </div>
        
        <!-- Tipo de Trámite -->
        <div class="col-lg-2 col-md-6 col-sm-6">
          <label class="form-label">
            <i class="fas fa-file-alt me-2"></i>Tipo
          </label>
          <select id="filterTipo" class="form-control">
            <option value="">Todos</option>
          </select>
        </div>
        
        <!-- Status -->
        <div class="col-lg-2 col-md-6 col-sm-6">
          <label class="form-label">
            <i class="fas fa-info-circle me-2"></i>Status
          </label>
          <select id="filterStatus" class="form-control">
            <option value="">Todos</option>
          </select>
        </div>
      </div>
      
      <!-- Botones de filtros -->
      <div class="d-flex justify-content-between align-items-center mt-3">
        <div class="d-flex gap-2">
          <button id="btnAplicarFiltros" class="btn btn-primary-custom btn-sm">
            <i class="fas fa-filter me-2"></i>Aplicar Filtros
          </button>
          <button id="btnLimpiarFiltros" class="btn btn-outline-secondary btn-sm">
            <i class="fas fa-times me-2"></i>Limpiar
          </button>
        </div>
        
        <!-- Límite por página -->
        <div class="d-flex align-items-center gap-3">
          <span class="text-muted small">Por página:</span>
          <select id="limitSelect" class="form-select form-select-sm" style="width: auto;">
            <option value="10">10</option>
            <option value="25">25</option>
            <option value="50">50</option>
            <option value="100">100</option>
          </select>
        </div>
      </div>
    </div>

    <!-- Stats Cards -->
    <div class="row mb-3 g-2">
      <div class="col-lg-4 col-md-6 col-sm-12 col-12">
        <div class="stats-card">
          <div class="stats-icon bg-primary">
            <i class="fas fa-file-alt"></i>
          </div>
          <div class="stats-info">
            <span class="stats-number" id="totalTramites">-</span>
            <span class="stats-label">Total</span>
          </div>
        </div>
      </div>
      <div class="col-lg-4 col-md-6 col-sm-12 col-12">
        <div class="stats-card">
          <div class="stats-icon bg-warning">
            <i class="fas fa-clock"></i>
          </div>
          <div class="stats-info">
            <span class="stats-number" id="tramitesPendientes">-</span>
            <span class="stats-label">Pendientes</span>
          </div>
        </div>
      </div>
      <div class="col-lg-4 col-md-6 col-sm-12 col-12">
        <div class="stats-card">
          <div class="stats-icon bg-success">
            <i class="fas fa-check-circle"></i>
          </div>
          <div class="stats-info">
            <span class="stats-number" id="tramitesCompletados">-</span>
            <span class="stats-label">Completados</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Stats por Status -->
    <div class="row mb-4 g-2">
      <div class="col-12">
        <div class="row g-2">
          <div class="col-12 col-sm-6 col-md-2">
            <div class="stats-card">
            <div class="stats-icon" style="background:#dbeafe;color:#1e40af;">
              <i class="fas fa-inbox"></i>
            </div>
            <div class="stats-info">
              <span class="stats-number" id="statIngresado">-</span>
              <span class="stats-label">Ingresado al área</span>
            </div>
            </div>
          </div>

          <div class="col-12 col-sm-6 col-md-2">
            <div class="stats-card">
            <div class="stats-icon" style="background:#fef3c7;color:#d97706;">
              <i class="fas fa-people-arrows"></i>
            </div>
            <div class="stats-info">
              <span class="stats-number" id="statTurnado">-</span>
              <span class="stats-label">Turnado a técnico</span>
            </div>
            </div>
          </div>

          <div class="col-12 col-sm-6 col-md-2">
            <div class="stats-card">
            <div class="stats-icon" style="background:#fce7f3;color:#be185d;">
              <i class="fas fa-pen-fancy"></i>
            </div>
            <div class="stats-info">
              <span class="stats-number" id="statFirma">-</span>
              <span class="stats-label">En firma (Jefe Dpto.)</span>
            </div>
            </div>
          </div>

          <div class="col-12 col-sm-6 col-md-2">
            <div class="stats-card">
            <div class="stats-icon" style="background:#e0e7ff;color:#7c3aed;">
              <i class="fas fa-share-square"></i>
            </div>
            <div class="stats-info">
              <span class="stats-number" id="statDireccion">-</span>
              <span class="stats-label">Turnado a dirección</span>
            </div>
            </div>
          </div>

          <div class="col-12 col-sm-6 col-md-2">
            <div class="stats-card">
            <div class="stats-icon" style="background:#f3e8ff;color:#a855f7;">
              <i class="fas fa-archive"></i>
            </div>
            <div class="stats-info">
              <span class="stats-number" id="statResguardo">-</span>
              <span class="stats-label">Resguardo para notificar</span>
            </div>
            </div>
          </div>

          <div class="col-12 col-sm-6 col-md-2">
            <div class="stats-card">
            <div class="stats-icon" style="background:#dcfce7;color:#15803d;">
              <i class="fas fa-flag-checkered"></i>
            </div>
            <div class="stats-info">
              <span class="stats-number" id="statNotificado">-</span>
              <span class="stats-label">Notificado</span>
            </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Cards Section -->
    <div class="table-container">
      <!-- Loading indicator -->
      <div id="loadingIndicator" class="text-center py-4" style="display: none;">
        <div class="spinner-border text-primary" role="status">
          <span class="visually-hidden">Cargando...</span>
        </div>
        <p class="mt-2 text-muted">Cargando trámites...</p>
      </div>

      <div id="tableContent">
        <!-- Tracking cards container -->
        <div id="tablaTramites">
          <!-- Los registros se cargarán dinámicamente aquí -->
        </div>

        <!-- Pagination -->
        <div class="d-flex justify-content-between align-items-center mt-3">
          <div class="d-flex align-items-center gap-2">
            <span class="text-muted small">Mostrando</span>
            <select id="limitSelect" class="form-select form-select-sm" style="width: auto;">
              <option value="10">10</option>
              <option value="25">25</option>
              <option value="50">50</option>
              <option value="100">100</option>
            </select>
            <span class="text-muted small">registros por página</span>
            <span id="rangeInfo" class="text-muted small ms-3"></span>
          </div>

          <nav aria-label="Paginación de trámites">
            <ul class="pagination pagination-sm mb-0" id="pagination">
              <!-- La paginación se generará dinámicamente -->
            </ul>
          </nav>
        </div>
      </div>

      <!-- No results message -->
      <div id="noResults" class="text-center py-5" style="display: none;">
        <i class="fas fa-search fa-3x text-muted mb-3"></i>
        <h5 class="text-muted">No se encontraron resultados</h5>
        <p class="text-muted">Intenta ajustar los filtros de búsqueda</p>
      </div>
    </div>
    </div>

<!-- Modal para Registrar/Editar Trámite -->
<div class="modal fade" id="modalTramite" tabindex="-1" role="dialog" aria-labelledby="modalTitulo" aria-hidden="true">
  <div class="modal-dialog modal-xl" role="document">
    <form id="formTramite">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="modalTitulo">
            <i class="fas fa-plus me-2"></i>
            Nuevo Trámite
          </h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>

        <div class="modal-body">
          <!-- Información del Trámite -->
          <h6 class="text-primary mb-3">
            <i class="fas fa-file-alt me-2"></i>
            Información del Trámite
          </h6>
          <div class="row g-3 mb-4">
            <div class="col-md-6">
              <label class="form-label">Folio de Oficialía *</label>
              <input type="text" id="folioOficialia" name="folioOficialia" class="form-control" placeholder="Ingresa el folio" required>
            </div>
            
                         <div class="col-md-6 empresa-container">
                           <label class="form-label">Empresa (código o nombre) *</label>
                           <input type="hidden" id="empresaId" name="empresa">
                           <input type="text" id="empresa" class="form-control" placeholder="Escribe código (p. ej., DGRI...) o nombre" required autocomplete="off">
                           <div id="empresaSuggestions" class="suggestions-dropdown" style="display: none;"></div>
                         </div>
            
            <div class="col-md-6">
              <label class="form-label">Fecha de Entrada</label>
              <input type="date" id="fechaEntrada" name="fechaEntrada" class="form-control">
            </div>
            
            <div class="col-md-6">
              <label class="form-label">Tipo de Trámite *</label>
              <select id="tipoTramite" name="tipoTramite" class="form-control" required>
                <option value="">Seleccionar tipo</option>
              </select>
            </div>
            
            <div class="col-md-6">
              <label class="form-label">Asunto Específico *</label>
              <select id="asuntoEspecifico" name="asuntoEspecifico" class="form-control" required>
                <option value="">Seleccionar asunto</option>
              </select>
            </div>
            
            <div class="col-md-6">
              <label class="form-label">Status</label>
              <select id="status" name="status" class="form-control">
                <option value="">Seleccionar status</option>
              </select>
            </div>
            
            <div class="col-md-6">
              <label class="form-label">Número de Páginas</label>
              <input type="number" id="numeroPaginas" name="numeroPaginas" class="form-control" min="1">
            </div>
            
            <div class="col-md-6">
              <label class="form-label">Fecha Estimada de Salida</label>
              <input type="date" id="tiempoEstimadoSalida" name="tiempoEstimadoSalida" class="form-control">
            </div>
          </div>
          
          <!-- Separador -->
          <hr class="my-4">
          
          <!-- Técnicos y Observaciones -->
          <h6 class="text-primary mb-3">
            <i class="fas fa-users me-2"></i>
            Asignación y Detalles
          </h6>
          <div class="row g-3">
            <div class="col-md-12">
              <label class="form-label">Técnicos Asignados</label>
              <div id="tecnicosLista" class="row g-2"></div>
            </div>
            
            <div class="col-md-12">
              <label class="form-label">Observaciones</label>
              <textarea id="observaciones" name="observaciones" class="form-control" rows="4" placeholder="Observaciones adicionales..."></textarea>
            </div>
          </div>
        </div>

        <div class="modal-footer">
          <button type="submit" class="btn btn-primary-custom">
            <i class="fas fa-save me-2"></i>
            Guardar Trámite
          </button>
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
            <i class="fas fa-times me-2"></i>
            Cancelar
          </button>
        </div>
      </div>
    </form>
  </div>
</div>

  </main>
</div>

<!-- Scripts -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/components/sidebar.js?v=1.2"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script src="/socket.io/socket.io.js"></script>
<script src="/js/gestionambiental/tramites.js?v=2.2"></script>
